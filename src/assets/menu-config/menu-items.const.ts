import {APP_CONF} from '../rest/app-conf.const';
export const MENU_ITEMS = [
  {name: APP_CONF.ROUTES.POSTS.name, url: APP_CONF.ROUTES.POSTS.url},
  {name: APP_CONF.ROUTES.USERS.name, url: APP_CONF.ROUTES.USERS.url}
];
